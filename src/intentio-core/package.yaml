_common: !include "../package-common.yaml"

name: intentio-core

<<: *meta

dependencies:
  - aeson
  - base
  - containers
  - convertible
  - lens
  - mtl
  - protolude
  - text
  - transformers

library:
  source-dirs: src

tests:
  intentio-core-test:
    main: Spec.hs
    source-dirs: test
    dependencies:
      - intentio-core
      - hspec
      - QuickCheck
