_common: !include "../package-common.yaml"

name: intentio-testutil

<<: *meta

dependencies:
  - aeson
  - aeson-pretty
  - base
  - directory
  - filepath
  - hspec
  - intentio-core
  - megaparsec
  - text

library:
  source-dirs: src
