import oparetions-lib:add
import oparetions-lib:sub
import oparetions-lib:mul
import oparetions-lib:div

fun main() {
  a = scanln();
  b = scanln();
  println(add(a, b));
  println(sub(a, b));
  println(mul(a, b));
  println(div(a, b));
}