# - run:
#     stdin:
#       - "5"
#     stdout: 
#       - "120"

fun main() {
    n = int(readln());
    println(str(factorial(n)));
}

fun factorial(n) {
    if(n < 0) { 
        println("The number should be non-negative"); 
        return fail; 
    }
    if(n == 0) { return 1; }
    factorial(n - 1) * factorial(n - 2);
}
