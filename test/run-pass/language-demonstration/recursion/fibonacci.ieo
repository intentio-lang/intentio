# - run:
#     stdin:
#       - "8"
#     stdout: 
#       - "21"

fun main(n) {
    if (n < 0) { println("The number should be positive"); }
    if (n == 0) { return 0; }
    if (n == 1) { return 1; }
    return main(n - 1) + main(n - 2);
}
