import check-classic-notation:check_classic_notation
import classic-notation-to-rnp:rnp
import rnp-to-classic-notation:classic_notation

fun main() {
    while(input = scanln()) {
        if(check_classic_notation(input)) {
            rnp = rnp(input);
            # checking the correctness of the function
            if(input == classic_notation(rnp)) {
                println("rnp: " + rnp);
            } else {
                println("Error");
            }
        } else {
            println("Incorrect input; please use only proper expressions in classic notation build with: (,),*,+,a,b,c");
        }
    }
}
