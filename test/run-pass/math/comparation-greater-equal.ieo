# - run:
#     stdout:
#       - "succ"
#       - "fail"

fun main() {
  a = 2;
  if(a >= a) { println("succ"); } else { println("fail"); };
  if(a >= a + 1) { println("succ"); } else { println("fail"); };
}
